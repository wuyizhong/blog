代码没有大的改动，启动之后form表单的参数正常提交，但是Controller中方法的参数接受不到参数，request中也没有参数。

各种尝试调试，最后选择尝试更换tomcat，实验成功。

考虑到更换tomcat的原因：

1.从抓包的内容看，参数已经正常提交给后台，但是后台没有接收到。

2.同事的电脑跑代码库的代码，是没有问题的，基本排除项目配置问题。

3.request的参数装载并不是特别了解，但是经过调试，最上层的拦截器中的参数也是空的，不是项目修改时候把参数弄丢。因此考虑先换个tomcat试试。

想起来两周前调试一个接口的post size大小，然后把server.xml的maxPostSize设置为0之后忘记改回来。
